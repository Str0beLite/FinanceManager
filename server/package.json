{
  "name": "finance-manager-connector",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Plaid connector for Finance Manager. Deployed separately from the app.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.100.0"
  }
}
